# Changelog

## 0.1.1

- First public release on npm.
- `repository` / `homepage` / `bugs` metadata now point to this public repo and the
  documentation site (previously referenced the internal monorepo).

## 0.1.0

- Initial build: `auth` (login/whoami/logout/env), `theme` (delegated to the theme
  toolkit), `integration` (scaffold/push/logs), `webhooks` (list/topics/deliveries/test).
- Self-contained, zero-dependency bundle.
